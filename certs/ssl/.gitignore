# SSL certificate and private key files (generated by scripts/gen-root-ca.sh)
# Commit the placeholder directory but never commit keys or signed certs.
certs/ssl/*.key
certs/ssl/*.crt
certs/ssl/*.csr
certs/ssl/*.srl
certs/ssl/*.pem
